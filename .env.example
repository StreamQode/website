# Set from Platform Terraform's frontdoor_endpoint_hostname output after the
# first Azure apply. This is the Front Door default endpoint, not Mission Control.
NUXT_PLATFORM_API_ORIGIN=https://YOUR-FRONTDOOR-ENDPOINT.azurefd.net
