# Copy to .env for local dev (already gitignored). In Vercel, set these as Project
# Environment Variables (same names).
NUXT_PUBLIC_API_BASE=https://core.streamqode.com
NUXT_PUBLIC_DEFAULT_TENANT=firstmotors
